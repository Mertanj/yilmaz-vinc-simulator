{
  "name": "yilmaz-vinc-simulator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "spike": "node spikes/01-pendulum-and-cable-force.mjs",
    "sahne": "esbuild tools/headless.ts --bundle --platform=node --format=esm --outfile=node_modules/.cache/sahne.mjs --log-level=warning && node node_modules/.cache/sahne.mjs"
  },
  "dependencies": {
    "pixi.js": "^8.20.1",
    "planck": "^1.4.2"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.9.0",
    "vite": "^7.0.0"
  }
}
